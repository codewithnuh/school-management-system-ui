import Typography from "@mui/material/Typography";

const Admin = () => {
  return (
    <div>
      <Typography variant="h4" color="white">
        Admin Dashboard
      </Typography>
    </div>
  );
};

export default Admin;
